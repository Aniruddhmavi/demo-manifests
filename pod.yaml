apiVersion: v1
kind: Pod
metadata: 
  name: argo_blankpage
  namespace: blankv1
spec:
  containers:
    - name: argo_blank
      image: anirudh632/fbclone:v3
      ports:

        - containerPort: 80
